# API mode: "mock" (default) or "real"
NEXT_PUBLIC_API_MODE=mock

# Enable direct demo bypass login buttons (true/false)
NEXT_PUBLIC_DEMO_BYPASS=false

# Base URL for real API (only used when API_MODE=real)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
